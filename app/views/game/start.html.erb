<div class="jumbotron">
  <div class="container">
    <h2>He Said / She Said What?! Quiz</h2>
    <p>Think you know the shit America's leading politicians <em>actually</em> say? Decide whether the politican and quote below go together to test your politicial know-how. You'll also find out who's totally bullshitting We The People – and who's not. </p>

    <h3><strong>Score: <strong> <%= session[:score].inspect %> out of <%= session[:count].inspect %> <%= link_to "Start a new game", game_start_path, method: :post, class: "btn btn-primary btn-lg" %></button></h3>


    <h3><% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>"><%= value %></div>
    <% end %></h3>

    <h3><%= @politician.name %></h3>
    <%= image_tag @politician.photo_url %>
    <h4><%= @quote.direct_quote %><h4>

 <div class="btn_left">
      <%= form_tag game_check_path do %>
      <%= hidden_field_tag "quote_id", @quote.id %>
      <%= hidden_field_tag "politician_id", @politician.id %>
      <%= hidden_field_tag "said_it", true %>
      <%= hidden_field_tag "said_it", true %>
      <%= submit_tag "Said it!", class: "btn btn-primary btn-lg" %>
      <% end %>
</div>

<div class="btn_right">
      <%= form_tag game_check_path do %>
      <%= hidden_field_tag "quote_id", @quote.id %>
      <%= hidden_field_tag "politician_id", @politician.id %>
      <%= hidden_field_tag "didnt_say_it", true %>
      <%= submit_tag "Didn't say it!", class: "btn btn-primary btn-lg" %>
      <% end %>
</div>


    </div>
  </div>
